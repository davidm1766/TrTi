<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sk.listok.zssk.zssklistok">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/appicon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />


        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <application android:name=".YourApplication" />

        <activity android:name=".activities.FindTrainsActivity" />
        <activity android:name=".activities.SelectTrainActivity" />
        <activity android:name=".activities.SelectPassengerTypeActivity" />
        <activity android:name=".activities.SelectPassengerInfoActivity" />
        <activity android:name=".activities.SelectFinishPayActivity" />
        <activity android:name=".activities.MyTicketsActivity" />
        <activity android:name=".activities.Reklama"></activity>
    </application>

</manifest>